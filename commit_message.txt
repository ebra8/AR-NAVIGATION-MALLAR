feat(navigation): overhaul user onboarding flow, settings UX, and optimize app startup

- Changed app windowBackground to prevent white flash on cold start.
- Implemented WelcomeScreen with 'Skip' and 'Phone Auth' routing.
- Added smart routing in MainActivity to track First Launch states via SharedPreferences and bypass Permissions if already granted.
- Created a dedicated SettingsScreen, replacing the old dropdown menu.
- Fixed UI lag on LogoScanScreen by migrating TFLite model and JSON graph loading to Dispatchers.IO.
- Added 'double-tap to exit' BackHandler logic to prevent users from looping back to Splash/Welcome screens.
- Fixed status bar contrast issues adjusting light/dark mode across specific screens.
- Added a loading indicator and auto-transition to the SplashScreen for returning users.
